
package Codigo;
//Problemas:
//1- Dividir el vector hasta encontrar unidades
//2- Condiciones de recursividad
public class DivideYVenceras {

    public boolean tabladivide_Venceras(String[][] tabla) {
        //Solución ecuaciones polinomicas cualesquiera que sean
        //Puntos a y b son continuos, es decir, se obtiene resultado
        //Punto medio entre a y b y se evalua en esa
        //Si es igual a =0, encontramos la raiz buscada
        // Sino, verificar signos
        // Definir intervalo [a,m][m,b]
        //continuar hasta acabar 
        if(1==1) {
            return true;
            // Fin de la recursión
        }else {
            // Llamadas recursivas
            // Combinación de subproblemas
            return false;
        }
    }
}
